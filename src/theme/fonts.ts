import { Quicksand, Exo_2 } from "next/font/google";

export const quicksand = Quicksand({
  subsets: ["latin"],
  display: "swap",
});

export const exo = Exo_2({
  subsets: ["latin"],
  display: "swap",
});
